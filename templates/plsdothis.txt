Put these bottom of body in all documents other than the login/reg pages

<script>
        const profileButton = document.getElementById('profileButton');
        const profileMenu = document.getElementById('profileMenu');
        profileButton.addEventListener('click', e => { e.stopPropagation(); profileMenu.classList.toggle('open'); });
        document.addEventListener('click', () => { profileMenu.classList.remove('open'); });
</script>

Put these under the appContainer in all documents other than the login/reg pages
<div id="profileMenuContainer">
            <button id="profileButton" aria-label="User Profile">
                <img class="profile-pic" src="{{ url_for('static', filename=user.avatar or 'avatar.png') }}" alt="{{ user.username }}">
            </button>
            <div id="profileMenu">
                <ul>
                    <li><a href="/profile/{{ user.id }}">Profile</a></li>
                    <li><a href="/logout">Logout</a></li>
                </ul>
            </div>
        </div>
Put these 2 near the bottom b4 the other <script> tags
<img id="decoyImage" src="/static/123.png" style="display:none;position:fixed;top:0;left:0;width:100vw;height:100vh;object-fit:cover;z-index:9999;">
<script>
         const root = document.documentElement;

         function toggleAway() {
           root.classList.toggle('away');
         }

         document.addEventListener('keydown', function (event) {
           if (event.ctrlKey && event.key.toLowerCase() === 'i') {
             event.preventDefault();
             toggleAway();
           }
         });
      </script>
